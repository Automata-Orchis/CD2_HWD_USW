# --- API server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
PyYAML==6.0.2

# --- PDF → 페이지별 PNG 분할 (다중 페이지 신청서) ---
# pure wheel + 내장 PDFium, poppler 등 시스템 바이너리 의존 없음 → 폐쇄망/non-sudo 환경에 적합.
pypdfium2==4.30.0

# --- PyTorch (cu121 wheels — driver 535.288.01 / CUDA 12.x 호환) ---
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# --- ML stack (transformers 5.9.0 검증 환경에서 사용된 버전) ---
accelerate==1.13.0
Pillow==10.3.0
safetensors==0.7.0
tokenizers==0.22.2
huggingface_hub==1.16.1
hf_transfer==0.1.9

# transformers 는 PyPI 가 아닌 git tag(v5.9.0) 핀이라 bootstrap.sh 에서 별도 처리.

# --- DB 조회용 CLI (sqlite3 binary 부재 환경 대응 — `python -m sqlite_utils` 로 호출) ---
sqlite-utils==3.38
